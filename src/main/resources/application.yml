server:
  port: 3000

spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

  datasource:
    url: jdbc:postgresql://${POSTGRES_HOST:localhost}:5432/sporterbook
    username: postgres
    password: 123456

jwt:
  token:
    secret: TestSecret
    expired: 3600000 #for production use 10-15 min

common:
  cors-ip: http://localhost:8080
  path: uploads
  news-file-dir: uploads/img/news

